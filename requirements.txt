# Core audio processing
numpy>=1.21.0
scipy>=1.8.0
librosa>=0.10.0
soundfile>=0.12.0

# Deep learning (stem separation)
torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0

# Web server
flask>=2.0.0

# Professional audio effects (Spotify/JUCE engine — same as pro DAWs)
pedalboard>=0.9.0

# Spectral noise reduction (clean up Demucs stem bleed before processing)
noisereduce>=3.0.0

# Reference mastering (match output loudness/EQ to a professional reference)
matchering>=2.0.0

# High-quality time-stretching and pitch-shifting
pyrubberband>=0.3.0

# Optional extras
matplotlib>=3.7.0
