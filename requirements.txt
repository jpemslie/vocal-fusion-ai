# Core audio processing
numpy>=1.21.0
scipy>=1.8.0
librosa>=0.10.0
soundfile>=0.12.0

# Deep learning (stem separation)
torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0

# AI-powered stem separation (BS-Roformer, MDX-Net — much cleaner than Demucs)
audio-separator>=0.24.0
onnxruntime>=1.19.0

# Web server
flask>=2.0.0

# Professional audio effects + time-stretch + pitch-shift (Spotify/JUCE engine)
pedalboard>=0.9.0

# True LUFS loudness measurement (ITU-R BS.1770-4)
pyloudnorm>=0.1.0

# Spectral noise reduction (removes Demucs bleed artifacts from vocal stem)
noisereduce>=3.0.0

# AI Director — uses Claude API to reason about mix quality and make decisions
anthropic>=0.20.0

# Neural vocal restoration (perceptual enhancer trained on speech/music)
# resemble-enhance>=0.0.1  # optional — large download (~600MB), enables neural vocal enhancement
