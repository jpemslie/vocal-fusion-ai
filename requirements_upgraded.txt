# VocalFusion — upgraded runtime requirements
# Install with: pip install -r requirements_upgraded.txt
#
# Core inference (already installed — pinned for reproducibility)
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.37.0
accelerate>=0.26.0
demucs>=4.0.0
deepfilternet>=0.5.6

# Audio I/O
soundfile>=0.12.1
librosa>=0.10.1
scipy>=1.11.0
numpy>=1.24.0

# NEW: Bayesian optimiser (replaces hill-climb)
optuna>=3.5.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Observability (optional but recommended)
prometheus_client>=0.20.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Autonomous songwriter (optional — only needed for autonomous_songwriter.py)
# Bark is included via transformers; MusicGen likewise
# Uncomment if you want the autogen pipeline:
# encodec>=0.1.1
